{
  "name": "meta-mcp-2",
  "version": "0.1.0",
  "description": "An xmcp application with tool syntax",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "xmcp build --cf",
    "dev": "npm run build && concurrently \"xmcp dev --cf\" \"npx wrangler dev\"",
    "start": "node dist/http.js",
    "deploy": "npm run build && npx wrangler deploy",
    "preview": "npm run build && npx wrangler dev",
    "test": "vitest run"
  },
  "dependencies": {
    "jose": "^6.2.2",
    "xmcp": "0.6.5",
    "zod": "^4.0.0"
  },
  "type": "module",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "@types/node": "^24.10.1",
    "concurrently": "^9.2.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.61.1"
  }
}
